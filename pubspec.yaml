name: kkcoder_mobile
description: "KKCODER Mobile - Remote Claude Code Terminal"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.0
  http: ^1.2.0
  flutter_secure_storage: ^10.0.0
  provider: ^6.1.0
  google_fonts: ^6.0.0
  webview_flutter: ^4.10.0
  flutter_markdown: ^0.7.0

dependency_overrides:
  webview_flutter_android: 4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
