class AppConfig {
  static const connectionTimeout = Duration(seconds: 10);
  static const wsReconnectDelay = Duration(seconds: 3);
  static const flushIntervalMs = 16;
  static const maxReplayBuffer = 3000;
  static const appName = 'KKCODER Mobile';
}
